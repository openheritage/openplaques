<h1>To do: Plaques with photos but no colours</h1>
<p><%= link_to("All to-do lists", "/todo") %></p>

<% if @plaques.size > 0 %>

	<p>These plaques should be easy to identify the colour for, as they have photographs:
	<ul class="photographer_photos">
	  <% @plaques.each do |plaque| %>
		<li><%= link_to(image_tag(plaque.photos.first.file_url, {:alt => ""}), "/plaques/" + plaque.id.to_s + "/colour/edit") %></li>
	  <% end %>
	</ul>
	</p>
<% else %>	
	<p>No plaques found.</p>
<% end %>
	<p class="clear"><%= link_to("All to-do lists", "/todo") %></p>
