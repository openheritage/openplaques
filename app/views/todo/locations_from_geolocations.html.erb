<h1>To do: Plaques with geolocations but no areas defined</h1>
<p><%= link_to("All to-do lists", "/todo") %></p>

<% if @plaques.size > 0 %>
	<p>These plaques have been geolocated, but not added to an location:
	<ul>
	  <% @plaques.each do |plaque| %>
		<li><%= link_to(plaque.first_person || "Plaque #" + plaque.id.to_s, "/plaques/" + plaque.id.to_s + "/location/edit")  %></li>
	  <% end %>
	</ul>
	</p>
<% else %>	
	<p>No plaques with geolocations but no areas defined!</p>
<% end %>	
<p class="clear"><%= link_to("All to-do lists", "/todo") %></p>